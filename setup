*venv setup (in powershell)
python -m venv .venv

(in bash)
source .venv/Scripts/activate

*packages
pip3 install flask flask-sqlalchemy pandas graphviz

*runs a local sever
python app.py

*venv deactivate
deactivate

need to create a uploads file in the SVD directory

git add --all (for all files)
git commit -m "some message" (message could be anything)
git push origin main